---
id: dependencies-existing
title: Prefer the existing stack
axis: dependency-posture
category: dependencies
section: engineering
applies-when:
  - choosing an implementation dependency
---
## Prefer the existing stack

Prefer the language runtime and dependencies already present in the project. Add a dependency only when the existing stack cannot express the behavior without substantial custom machinery.
